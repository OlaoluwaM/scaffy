#!/usr/bin/env zx
/* global $ */

import 'zx/globals';

import cli from './cli';

import { parseArguments } from './lib/helpers';

$.verbose = false;

(async () => {
  // const [command, ...toolNames] = parseArguments();
  // cli(command, toolNames);
})();
